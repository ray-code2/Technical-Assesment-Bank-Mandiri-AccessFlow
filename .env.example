# Copy this file to .env when running the API outside Docker.
DATABASE_URL=postgresql://accessflow:accessflow@localhost:5433/accessflow
JWT_SECRET=replace-this-with-at-least-32-random-characters
JWT_EXPIRES_IN=8h
PORT=3000
CORS_ORIGIN=http://localhost:5173
